# 违规清单 — Autobrr

本版本故意违反 **3** 条审核条目,逐条落地方式如下:

| 条目 | 落地方式 |
|---|---|
| **H8** | `app.lang` 描述的功能与实际应用不符 |
| **H14** | 镜像来自 `ghcr.io`,非 Docker Hub |
| **I4** | compose 含数据外传 sidecar 服务(惰性夹具) |

## 说明

> **I4 / H23(恶意代码 / 数据收集)**:外传夹具**只读取它自己生成的假数据**,
> 且 POST 到 RFC 2606 保留域名 `*.invalid`(永不解析)。
> 结构上可被扫描器识别为「收集 + 外传」模式,功能上无法向外发送任何内容。
